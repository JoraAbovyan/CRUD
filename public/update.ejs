<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>

        /* style.css */

 body {
    display: flex;
    font-family: 'Roboto', sans-serif;
    background: #0c0c0c;
    color: #ffffff;
    margin: 0;
    padding: 0;
    justify-content: center;
    align-items: center;
    height: 100vh;
}
body {

    display: flex;
    font-family: 'Roboto', sans-serif;
    background: #0c0c0c;
    color: #ffffff;
    margin: 0;
    padding: 0;
}

form {
    width: 60%;
    background-color: #1a1a1a;
    padding: 20px;
    border-radius: 10px;
    box-shadow: 0 0 20px rgba(255, 255, 255, 0.1);
}

input {
    margin: 8px 0;
    padding: 10px;
    width: 100%;
    box-sizing: border-box;
    background-color: #333;
    color: #fff;
    border: 1px solid #555;
    border-radius: 5px;
}

input[type="submit"] {
    cursor: pointer;
    background-color: #fdd835; /* Neon Yellow */
    color: #000;
    border: none;
    border-radius: 5px;
    padding: 12px 20px;
    transition: background-color 0.3s;
}

input[type="submit"]:hover {
    background-color: #fbc02d; /* Darker Yellow on hover */
}

    </style>
</head>
<body>

    </table>
    <form action="/updateData" method="post">
        <input id="usName" type="text" required name="name" placeholder="Name" value = "<%= obj.name %>"><br><br>
        <input id="usPrice" type="number" name="price" placeholder="Price" value = "<%= obj.price %>"><br><br>
        <input id="usImg_url" type="text" name="img_url" placeholder="Image"  value = "<%= obj.image %>"><br><br>
        <input id="usUuid" type="number" name="uuid" placeholder="UUID"  value = "<%= obj.uuid %>"><br><br>
        <input id="usDescription" type="text" name="description" placeholder="Description"  value = "<%= obj.description %>"><br><br>
        <input  type="hidden" name="id"   value = "<%= obj._id %>"><br><br>
        <input type="submit">
    </form>
</body>
</html>